# Tasks

- [x] Add Arduino Nano/SIM800L reference firmware and wiring documentation.
- [x] Generate QR payloads containing device ID and 128-bit claim credential only.
- [x] Parse and protect the claim credential in the Android app.
- [x] Send and verify the `P1`/`P2` pairing handshake.
- [x] Derive and securely persist the command key after a valid pairing acknowledgement.
- [x] Sign and verify `CMD`/`ACK` messages with replay counters.
- [x] Show unpaired, pairing, active, and failed states in the device UI.
- [x] Validate OpenSpec and build the Android debug APK.
